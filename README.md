# Práctica de Servicios REST Comparativa de implementación de una API CRUD utilizando Node.js (Express) y PHP (PDO) con una base de datos MySQL.

Endpoints probados:

GET /usuarios

POST /usuarios

PUT /usuarios/:id

DELETE /usuarios/:id
